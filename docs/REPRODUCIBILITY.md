# 复现记录与已知限制

## 已收到并审阅

- 六个云端训练/推理/对比/服务脚本；metadata/source_provenance.json 记录原始文件 SHA-256。
- 已完成实验的 adapter_config.json 与 trainer_state.json。
- 用户从 qwen-sft 环境导出的直接依赖版本。
- 源码说明：监督目标是最后一条 assistant 回复；之前的完整对话前缀被 mask 为 -100，超过长度导致没有监督 token 的样本被过滤。
- train.py 仍在过滤前检查第一条样本；若该样本被完全截断，会提前报错。此行为来自原代码，本次未将训练逻辑改写成另一套实现。

## 本次整理的改动

- 固定机器路径改为环境变量，默认指向仓库内 models / data/private / output。
- 服务移除默认密钥，要求显式设置 PCB_LLM_API_KEY，认证异常不能绕过。
- 非空训练输出目录和已有对比报告拒绝覆盖。
- 样本文本日志改为显式开启，训练数值统计保留。
- 增加无需加载模型的数据审查与预处理测试；没有在本机导入或运行 GPU 训练模块。
- 私有数据、权重、缓存、日志和本地配置被忽略。metadata 中的旧路径仅为原实验来源记录。

## 尚未验证或缺失

- 整理后的 FastAPI 服务尚未在 GPU 上重新部署；原云端服务已在应用联调中实际运行。本次仅改模型路径配置与空密钥拒绝逻辑，保留原推理和系统提示。
- 完整私有数据及其生成/清洗/划分脚本、授权说明。
- 精确基础模型 revision、tokenizer 文件哈希、Python/驱动版本。
- 训练集与验证集真实重叠和截断统计。
- 原自由回答 Base/LoRA 比较结果，以及应用固定证据任务的基础模型对照。
- 新环境 GPU 重训、整理后训练脚本和推理脚本的 GPU 运行验证。

基础模型、adapter 和完整数据需要在使用者本地提供。权重文件未进入 Git，本仓库不能被描述为“下载后无需准备即可训练”。

## 本轮本地验证

14 项无 GPU 测试通过，包括训练输出目录拒绝覆盖、assistant 标签屏蔽、截断过滤、数据格式/重复检查，以及空密钥启动拒绝、Bearer 认证检查。3 条合成格式样例检查通过（训练2条、验证1条），未发现样例间精确重复；该结果不适用于尚未收到的私有训练集。没有在本机重训模型。
